// gs_jsi.cpp — empty translation unit required by the JSI CMake target
//
// The actual implementation is header-only in gs_jsi.h.
// Compile with GIOVANNI_JSI_ENABLED=1 (set by CMakeLists.txt when jsi/jsi.h is found).

#ifdef GIOVANNI_JSI_ENABLED
#include "gs_jsi.h"
#endif